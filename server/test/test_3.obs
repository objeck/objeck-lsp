class Test {
	fie : static : Int;

	enum Foo {
		BAR
	}

	New() {

	}

	function : Main(args : String[]) ~ Nil {
		abc := args->Size();
		abc += 100;
		efg *= fie / (abc - args->Size());
		efg->PrintLine();


		
		bar := "Hello World";
		bar->Split("abc");
		meme := bar->Clone();

		

		o : Test := Test->New();
		
		o->Foo();
		
		t := o->Bar();
		t += 18;
		t->PrintLine();
	}

	function : Bar() ~ Int {
		return 99;
	}

	function : Foo() ~ Int {
		return 13 + 85;
	}
}