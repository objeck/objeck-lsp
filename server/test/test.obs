use Query.RegEx;
use System.IO.File;

class Test {
	function : Main(args : String[]) ~ Nil {
		text := FileReader->ReadFile("c:/users/objec/desktop/foo.txt");
		
		regex := RegEx->New("function");
		found := regex->Find(text)<String>;
		each(i : found) {
			found->Get(i)->PrintLine();
		};
	}
}