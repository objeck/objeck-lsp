class Hello {
	function : Main(args : String[]) ~ Nil {
		a := 7;
		twenty := a + 13 + 7;
		twenty->PrintLine();

		foo := "foo";
		bar := foo->SubString(1,2);
		bar->PrintLine();

		z := "foo";
		z->SubString(2,3);

		13->ToBinaryString();

		if(twenty <> 19) {
			select(3) {
				label 1: {

				}

				other {

				}
			};
			3->PrintLine();
		};
	}
}