class FooBar {
	New() {
		
	}
}

enum Fie {
	ONE,
	TWO
}

class ValueHolder {
	@value : Int;

	New(value : Int) {
		@value := GetValue(3);
	}

	method : public : GetValue(a : Int) ~ Int {
		return @value / a;
	}
}