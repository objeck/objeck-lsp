class Test {
	@abc : ValueHolder;

	New() {
		@abc := ValueHolder->New(7);
	}

	function : Main(args : String[]) ~ Nil {
		vh : ValueHolder := ValueHolder->New(13);
		vh->GetValue(5)->PrintLine();
		
		DoIt(7);

		t := Test->New();
		t->Bar()->PrintLine();

		f := Fie->ONE;
		xx := f + f;
	}

	method : Foo() ~ Int {
		return 7 + @abc->GetValue(12);
	}

	method : Bar() ~ Bool {
		gag := 13;
		gag := gag - @abc->GetValue(13)          -gag;
		return @abc = @abc;
	}
	
	function : DoIt(i : Int) ~ Int {
		return i * 13;
	}
}